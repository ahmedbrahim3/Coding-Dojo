function d6(){
var roll=Math.random()
roll=Math.floor(Math.random()*7)
return roll
}   
///////////////////////:
function randomFromArray(arr){
    var random=Math.floor(Math.random()*arr.length)
        var lifesAnswers=arr[random]
    return lifesAnswers
    }